[pytest]
markers =
    integration: requires the local OSRM service on 127.0.0.1:5000
    official: performs live requests to configured official HTML pages
    live: requires an external live source or deployment environment
